# Debt-Zero Sprint — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** retire every remaining autonomously-fixable ledger debt before Phase C — the residual security oracles, the floating SDK pin, the channel-collision class, the offline queue's silent-failure surface, and the dead-code/doc-rot batch.

**Architecture:** authority = the handoff doc §6 (`docs/superpowers/HANDOFF-2026-07-19-fable-to-opus.md`) + each item's originating ledger entry. Backend first (T1), then the two client reliability items (T2), then the sweep batch (T3). Spec-level detail lives in the handoff — implementers read it + the cited precedents.

**Tech Stack:** Postgres + pgTAP (private-schema pattern), Swift CI-only (supabase-swift Realtime, SwiftData queue), the established seam idioms.

## Global Constraints
- All laws in handoff §3 bind (append-only migrations, live-pg_policies inventories, no bpm logging, explicit staging, commit-don't-push, citations).
- Never `git add -A`.

## Task 1: Residual DEFINER-oracle relocation (backend)
- [ ] Queue order: `routine_has_active_session_for_user` FIRST (routine_id publicly discoverable via Discover), then `is_series_organizer`, `proposal_session_id`. Method per the Phase O sweep: inventory each helper from LIVE `pg_policies` + `pg_proc` prosrc + `information_schema.triggers`/views + `grep -rn "\.rpc(" supabase/functions/` (the Edge-Function check that caught is_session_participant). No RPC caller → relocate to `private` per 20260722000001; RPC caller found → dual-function treatment per 20260726000001.
- [ ] pgTAP per helper: dependent policies re-proven both directions + public-gone (or 42501) proofs; full suite TRUE totals; db push. Commit.

## Task 2: Channel-collision defensive fix + Package.resolved
- [ ] Read `HeartRateBroadcastService.publish/subscribe` + `SessionBroadcastService` + how supabase-swift's RealtimeClientV2 keys channels by topic (fetch SDK source via `gh api` — the Phase W gate's I-1 finding quotes the hazard: `client.channel(topic)` returns the EXISTING channel; `removeChannel` unsubscribes it — a disposable publish channel on a subscribed topic tears down the subscription). Fix: publish through a channel handle the service OWNS — if a subscription for the topic is already held, reuse it for broadcast; only create-and-remove a disposable channel when the topic is NOT held; never removeChannel a channel this code didn't create. Audit the T2-era watch soundboardTap path for the same class (SessionBroadcastService's channel for `session:{id}` — read how the tap publish works vs the view's subscription).
- [ ] Hermetic tests via the seam idioms where the channel bookkeeping is pure (held-topic registry logic); the actual SDK behavior is CI+device territory — honest notes.
- [ ] Commit `GymSyncApp/Package.resolved` (find where xcodegen/SPM writes it in CI — locally there's no Xcode, so: pin exact versions in project.yml instead if Package.resolved cannot be generated on this machine — ADJUDICATE with evidence: the honest goal is deterministic archive-time SDK resolution; `exactVersion` pins in project.yml achieve it without a Mac. State the choice.) Commit.

## Task 3: Reliability + dead-code batch
- [ ] Replay-failure toast: `OfflineSetLogQueue.lastPermanentFailure` gets a minimal observable surface — read how the queue exposes it + the app's banner idioms (GSInlineNoticeBanner from W); smallest honest UI (a dismissible notice on the session/home surface when a permanent drop occurred; record deviation). 
- [ ] `forceSignedOutAfterDeletion()` purges the deleted user's queued rows (they can never sync — account gone; cite the gate reasoning; keep signOut()'s non-purge doctrine comment intact).
- [ ] Dead code: `SessionCalendarSyncStore.load(defaults:)` (zero callers — delete); `routineLabel(for:)` placeholder (read what it should honestly display; fix or document); the pre-existing setIndex mis-citation (GroupSessionLiveView penalty path is line ~1766 not 2158 — fix the comment).
- [ ] Hermetic tests for the purge + toast state logic. Commit.

## Task 4 (controller): gate + merge.

## Self-Review
Handoff §6 items 1-6 → T1 (item 1), T2 (items 2+3), T3 (items 4-6), 1:1. Design-gated debt (sharing-paused, consent toggle, zones) correctly EXCLUDED — Phase D owns it. QA-gated I-1 confirmation unaffected: the defensive fix is correct under either QA outcome.
