[
  {
    "screenId": "tab-you",
    "reason": "You tab implements the newer Settings Hub with added stat tiles + Apple Health row — a recorded deviation ahead of the superseded canvas 'You' frame, awaiting design sign-off. Phase M Task 4 adds two more rows on top of that same deviation: a \"Share Solo Workouts\" toggle (with inline caption) inside the Settings group box, and a red \"Delete Account\" row below it — both extend this entry rather than opening a new one; awaiting sign-off alongside the rest. Phase H Task 2 adds one more row to the same Settings group box: \"Add my sessions to Calendar\" (default off, toggle-row-with-inline-caption shape matching \"Share Solo Workouts\"; system-denied state collapses the row into a single open-Settings tap target per `PTTDockRow.deniedRow`'s idiom) — no canvas frame depicts EventKit sync at all, so this extends the existing deviation rather than opening a new one; awaiting sign-off alongside the rest."
  },
  {
    "screenId": "exercise-detail",
    "reason": "Demo card shows a real mirrored photograph (free-exercise-db); the canvas frame 14 mock uses placeholder art — photographic content can never pixel-match a mock. Layout structure verified faithful (2026-07-16)."
  },
  {
    "screenId": "stattile-loading",
    "reason": "Catalog capture shows ONE forced state; canvas frame 41 is a four-state sheet — a constant structural offset is expected. Per-state fidelity verified in review (copy byte-exact)."
  },
  {
    "screenId": "stattile-error",
    "reason": "Catalog capture shows ONE forced state; canvas frame 41 is a four-state sheet — a constant structural offset is expected. Per-state fidelity verified in review (copy byte-exact)."
  },
  {
    "screenId": "stattile-empty",
    "reason": "Catalog capture shows ONE forced state; canvas frame 41 is a four-state sheet — a constant structural offset is expected. Per-state fidelity verified in review (copy byte-exact)."
  },
  {
    "screenId": "tab-stats",
    "reason": "Stats tab gains current + longest streak tiles (Phase S Task 5, `user_streaks`) — a system-designed addition (GSCard/GSSectionHeader/GSStatTile idiom, matching the weekly-volume/recent-PRs card rhythm already on this screen; flame glyph for a live streak) with no canvas frame of its own yet. Awaiting a designer frame; capture will need refreshing once one exists. Phase H Task 3 adds one more card below Recent PRs: \"Body Weight\" (`body_weight_logs`, master spec `:692`) — a GSSectionHeader + \"+ Log\" button (mirrors `RoutinesListView.yourRoutinesHeader`'s \"+ New\" shape) above the reused `TrendChartView` (same component `ExerciseHistoryView`'s Est. 1RM trend already uses) — extends this entry rather than opening a new one; awaiting sign-off alongside the rest."
  },
  {
    "screenId": "_streaks-group-abandoned-rule",
    "reason": "BEHAVIOR (not visual): migration 20260719000007 breaks the GROUP streak on a bare abandoned transition with an absent member — beyond Flow 7 literal text (group rule lists only no_show) but converging to product intent; removes an unenforced config-ordering dependency. Whole-branch review 2026-07-16 recommended recording it here for traceability."
  },
  {
    "screenId": "session-chat",
    "reason": "Phase F Task 3: no canvas frame depicts a session sub-thread chat affordance — proof-frame-05/06/07 (Lobby, Live Spotlight, Live Roster headers) show no chat button. System-designed: a bordered icon-button (LobbyView toolbar / GroupSessionLiveView headerBar, styled after each screen's own existing icon buttons) opening a sheet with the reused ChatView, text-only send row. Awaiting a designer frame."
  },
  {
    "screenId": "group-stats",
    "reason": "Phase F Task 5: GroupView's new Stats sub-tab (spec Flow 5, deferred from Phase 2) — collective SESSIONS/TOTAL LBS/PRS tiles, the group streak card (deferred from Phase S), and a per-member volume leaderboard. proof-frame-22.png ('Members') and proof-frame-19.png (already mapped to 'friends' in frame-map.json) were both rendered and inspected — neither depicts a collective-metrics/leaderboard/streak surface. System-designed: GSStatTile collective-tiles row mirrors Home's StatTilesRow 3-tile idiom; the streak card mirrors StatsTabView.streakCardView verbatim (swapped to the group-scoped read); leaderboard rows mirror GroupRecapView.leaderboardRow's shape minus rank/kudos (neither applies to an all-time aggregate outside a live session). Awaiting a designer frame."
  },
  {
    "screenId": "edit-profile",
    "reason": "Phase F Task 6: Edit Profile (display name + avatar), deferred from Phase 2.5 — the You-tab Edit button was inert until this task (task-2-report.md recorded it as a future screen; no canvas frame was ever produced for it). System-designed: avatar-picker row mirrors GroupView.membersList's shape (56pt preview + accent 'Change Photo' link, upload-on-pick), display-name field mirrors CreateGroupView's bordered-TextField shape with an explicit toolbar Save. Awaiting a designer frame."
  },
  {
    "screenId": "report-sheet",
    "reason": "Phase M Task 2 (moderation/compliance): the Report action's reason-picker + freeform-details sheet is an App Store review requirement (Guidelines 1.2/5.1.1), not a designed screen — no canvas frame exists or is expected. System-designed: fixed reason-category list (Harassment/Spam/Inappropriate content/Other) with a checkmark-selection row shape borrowed from AppearanceView's palette rows, freeform TextField + toolbar Cancel/Submit mirroring CreateGroupView's sheet shape."
  },
  {
    "screenId": "blocked-users",
    "reason": "Phase M Task 2 (moderation/compliance): the You-tab Blocked Users list is the same App Store compliance surface as report-sheet above — no canvas frame exists or is expected. System-designed: pushed List of Profile rows (GSInitialsAvatar + two-line name block, mirroring FriendsView's friends-list row shape) with a per-row Unblock button; GSEmptyState/GSErrorCard for the no-blocks/load-failed cases, matching this codebase's other list-screen idioms."
  },
  {
    "screenId": "delete-account",
    "reason": "Phase M Task 4 (moderation/compliance): the Delete Account typed-confirmation sheet is an App Store 5.1.1 compliance requirement, not a designed screen — no canvas frame exists or is expected. System-designed: sheet shape mirrors report-sheet's self-contained NavigationStack + toolbar Cancel; a red-accented warning card explains the irreversible cascade, a bordered TextField requires typing the exact string \"DELETE\" before the destructive confirm button (solid red, disabled until matched) becomes tappable. Catalog/screenshot capture only ever shows this pre-confirmation state — the confirm button's disabled guard makes an accidental live deletion structurally impossible from the capture harness; the actual deletion is device-QA only."
  },
  {
    "screenId": "tab-library",
    "reason": "Phase L Task 3 (Discover): LibraryTabView's segmented sub-tab control widened from 2 segments (Routines/Exercises) to 3 (+ Discover), per the master spec's four-sub-tab Library structure (`:690`; Campaigns stays Phase C). The `tab-library` frame's top segmented-control row shifts width/layout as a direct consequence — Routines (the default sub-tab, unchanged content below the control) is still what this capture shows. Awaiting a designer frame that reflects the 3-segment control."
  },
  {
    "screenId": "discover",
    "reason": "Phase L Task 3: Library's new Discover sub-tab (public workout repository grid, master spec Flow 4 / `:690`) — grepped `docs/design/frame-map.json` and every `.dc.html` canvas file for \"Discover\": zero hits, matching the Phase L design's own prediction (\"Discover may be undesigned -> system-designed + deviations\"). System-designed: 2-column grid (`SoundLibrarySheet.catalogGrid`'s LazyVGrid idiom) of cards mirroring `LibraryTabView.packCard`'s bordered-card shape (image placeholder, name, owner caption, FEATURED chip, scoring-metric chips, attempt-count caption when non-zero) — is_featured-first ordering, plus a new Top Lifters entry row at the top (Task 4). A catalog fixture (not a live/seeded capture) still backs the screenshot for determinism — Task 4 (2026-07-18) shipped the seed content (\"The Murph\" + 2 templates, `scripts/seed_qa_fixtures.js`) this entry previously flagged as not existing yet, so a live capture is now possible if a future task wants one; this fixture-driven capture was kept as-is (no mechanism change) since Task 4's checklist didn't ask for the capture harness itself to be re-architected. Awaiting a designer frame."
  },
  {
    "screenId": "discover-detail",
    "reason": "Phase L Task 3: the public workout detail screen reached from Discover (master spec Flow 4, `:790-797`) — same \"no canvas frame\" finding as \"discover\" above. System-designed: description + scoring-metric chips header, a read-only exercise list (row shape borrowed from `RoutineBuilderView.exerciseRow`'s name/muscle/equipment caption, minus its editable stat-tiles/drag-handle/remove chrome), Attempt Solo / Attempt with Friends CTAs, and a sortable leaderboard — segmented control reimplements `LibraryTabView.segmentedControl`'s flat-rectangle/1px-divider shape (that one is `private` to its own file and fixed at 2 segments; this screen needs a dynamic 2-4 depending on the routine's `scoring_metrics`), leaderboard rows mirror `GroupRecapView.leaderboardRow`'s rank+avatar+name+metric shape with `GSInitialsAvatar` swapped in for the plain-initials square and a \"✏️ edited\" indicator (Flow 6, `:836`) replacing the kudos chip. Report/Block toolbar reuses Phase M's `RoutineModerationToolbar` (extracted from `RoutineDetailChoice` for this exact purpose — this is the first screen where that menu is ever live, since Discover routines are never the viewer's own). Task 4 (2026-07-18) shipped the seed content this entry previously flagged as not existing yet (see \"discover\" above) — capture still fixture-driven, same reasoning. Awaiting a designer frame."
  },
  {
    "screenId": "top-lifters",
    "reason": "Phase L Task 4: the global cumulative-volume leaderboard reached from Discover's new \"Top Lifters\" entry row — same \"no canvas frame\" finding as \"discover\"/\"discover-detail\" above (grepped `docs/design/frame-map.json` + every `.dc.html` for \"Top Lifters\"/\"lifters\": zero hits). System-designed: bordered icon + title/subtitle + chevron entry row on Discover mirrors `ScheduleSessionView.whatSection`'s Menu-row shape; the board itself reuses `GroupRecapView.leaderboardRow`'s rank+avatar+name+metric idiom with `GSInitialsAvatar` swapped in for the plain-initials square (same substitution `discover-detail` already made) and a \"You\" row highlight (same theme-token tint `GroupRecapView.leaderboardRow` and `discover-detail`'s leaderboard rows both already use). No opt-in gate: `profiles` SELECT RLS is `USING (true)` for any authenticated user, so this board is a plain ordered+LIMIT-ed read, not a new relationship oracle. Awaiting a designer frame."
  },
  {
    "screenId": "body-weight-log",
    "reason": "Phase H Task 3: the Stats \"Body Weight\" card's log-entry sheet (`body_weight_logs`, master spec `:395-401` schema / `:692` Stats placement) — no canvas frame depicts a body-weight logging affordance at all. System-designed: sheet shape mirrors `ReportSheet`'s self-contained `NavigationStack` + toolbar Cancel/Save (Features/Moderation/ReportSheet.swift), the weight field itself reuses `LogSetSheet`'s shared bordered stepper-cell component verbatim (Features/Workout/LogSetSheet.swift:190-261) rather than a new bespoke input, so it matches every other weight entry already in the app. Unit is a fixed \"lbs\" default in v1 — no unit-preference setting exists anywhere in `profiles`/`user_settings` (confirmed by grep before this task). Awaiting a designer frame."
  },
  {
    "screenId": "offline-syncing-indicator",
    "reason": "Phase O Task 3 (offline-first set logging, master spec §6.4): a queued-but-not-yet-confirmed set log needs a small visual signal on both surfaces that render logged sets — WorkoutSessionView.loggedSetsTable (solo) and GroupSessionLiveView.feedRow (group live feed) — no canvas frame depicts an offline/syncing state on either. System-designed: reuses the exact GSTag(style: .outline) chip idiom this codebase already uses for compact per-row status labels (GroupSessionLiveView's own 'penalty'/'failed' tags on the same feedRow, and the plate-math entry above's per-side plate chips) for the group feed row ('syncing' tag alongside penalty/failed); the solo table's checkmark-column instead swaps to a small rotate-arrows glyph (SF Symbol arrow.triangle.2.circlepath) in neutral500, matching that column's existing icon-only shape (a checkmark) rather than adding a text chip to a 4-column numeric grid where one wouldn't fit. Judgment call: NO catalog case added. Every existing catalog case that reaches into a specific inline visual state (stattile-loading/error/empty, plate-math) does so because the state is otherwise unreachable deterministically (forced network conditions, a disclosure toggle) — this state depends on a live SwiftData-backed queue (OfflineSetLogQueue) inside a full session flow (a real routine + session for WorkoutSessionView, or a real live group session + participants + rotation for GroupSessionLiveView), neither of which has anything close to LogSetSheet's self-contained-sheet cost to fixture. The chip's own visual shape (GSTag outline style) is already exercised by the plate-math catalog case; only its text label and placement are new here, which hermetic tests + device QA (task-3-report.md's device-QA list) cover more proportionately than a bespoke fixture would. Awaiting a designer frame. Phase O Task 3 fix wave 1 (reviewer Finding 1, 2026-07-19) adds a second, related-but-distinct visible state: GroupSessionLiveView's my-turn spotlight previously fell through to an unconditional `advanceTurn` call after an offline set-log queue, which always threw offline and surfaced the existing `GSInlineErrorBanner` (\"Set didn't save\" — false; the set DID save locally — with a \"Try again\" retry that minted a duplicate queued row per tap). Fixed by skipping `advanceTurn` on that path and introducing a new sibling component, `GSInlineNoticeBanner` (DesignSystem/GSComponents.swift, directly below `GSInlineErrorBanner`) — same solid-accent icon+copy shape, checkmark glyph instead of exclamation-circle, no retry CTA — rendered with copy \"Saved on this phone. Your turn will pass once you're back online.\" Same judgment call as the syncing-chip above and for the same reason (live SwiftData queue + full group-session flow, not deterministically fixturable at LogSetSheet's cost): NO catalog case added; extends this entry rather than opening a new one, per the codebase's own established precedent (see the plate-math entry's \"Fix wave 1\" extension below)."
  },
  {
    "screenId": "plate-math",
    "reason": "Phase H Task 4: the log-set sheet's plate-loading disclosure — master spec unit-test list names \"Plate math (target weight + bar weight → plate stack)\" explicitly (`:1292`) but no canvas frame depicts a plate-math affordance anywhere; grepped `docs/design/frame-map.json` and every `.dc.html` canvas file for \"plate\": zero hits. System-designed: a compact icon+label+chevron \"Plates\" toggle button (no existing expand/collapse precedent in this codebase to mirror) sits directly below the existing Reps/Weight stepper row inside `LogSetSheet` (Features/Workout/LogSetSheet.swift), one shared implementation reused by both the solo (`WorkoutSessionView.swift:224`) and group-penalty (`GroupSessionLiveView.swift:1399`) presentations of that sheet. The expanded disclosure renders the per-side plate breakdown as `GSTag(style: .outline)` chips (same tag component `GroupSessionLiveView`'s penalty/failed labels already use, `GSComponents.swift:152`) plus a remainder caption when the target isn't exactly reachable. Hidden entirely for empty/invalid weight, matching `BodyWeightLogSheet.canSubmit`'s `Decimal(string:)` idiom (Phase O Task 2: this parse idiom is now the shared `Decimal.parseUserInput(_:)` helper, Models/Decimal+ParseUserInput.swift — parse-side locale fix only, no visual change). Awaiting a designer frame. Phase H Task 4 fix wave 1 (inline-card extension) promoted the expanded disclosure body itself out of `LogSetSheet` into the free-standing `PlateStackDisclosure` view (`LogSetSheet.swift:397`) specifically so `GroupSessionLiveView`'s inline \"LOG THIS SET\" card (`logThisSetCard`, `GroupSessionLiveView.swift:~935`) could render the SAME disclosure directly inline — a THIRD presentation beyond the two named above, live during a group session's turn-based flow rather than inside a `LogSetSheet` sheet at all. Same component, same hidden/inert-for-empty/invalid/non-positive-weight gate, same deviation — extends this entry rather than opening a new one."
  }
]
