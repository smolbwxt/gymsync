import Foundation

// Copy to Secrets.swift (gitignored) and fill in real values.
enum Secrets {
    static let supabaseURL = URL(string: "https://YOUR-PROJECT.supabase.co")!
    static let supabaseAnonKey = "YOUR_PUBLISHABLE_KEY"

    // Phase O Task 4 (master spec §6.8.5) — OPTIONAL. Empty string (the
    // default here and in CI when the SENTRY_DSN repo secret doesn't
    // exist yet) means Sentry is fully disabled: CrashReporting.start(...)
    // never calls SentrySDK.start at all (Services/CrashReporting.swift).
    // USER ACTION: create a Sentry project, then set this to its DSN
    // locally and add a SENTRY_DSN repo secret in GitHub for CI/TestFlight
    // builds (see .github/workflows/ios.yml's "Write Secrets.swift" steps).
    static let sentryDSN = ""
}
