import Foundation

// Copy to TestSecrets.swift (gitignored) and fill in real values to run
// authenticated network tests locally. CI writes this from repo secrets.
enum TestSecrets {
    static let testUserEmail = "PLACEHOLDER"
    static let testUserPassword = "PLACEHOLDER"
}
